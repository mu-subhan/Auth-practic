<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users List</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-gray-800">
    <a href="/" class="text-blue-500 hover:underline px-6">Go back to Home</a>
    <div class="container mx-auto p-6">
        <h1 class="text-2xl font-bold mb-4 text-white">Registered Users</h1>
        <ul class="bg-white shadow-md rounded-lg ">
            <% users.forEach(user => { %>
                <li class="border-b p-4 hover:bg-gray-50">
                    <span class="font-semibold"><h2>Name :</h2><%= user.username %></span> 
                    (Email: <%= user.email %>, Age: <%= user.age %>)
                </li>
            <% }); %>
        </ul>
        <div class="mt-4">
        </div>
    </div>
</body>
</html>
